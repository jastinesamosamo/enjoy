workflows:
  android-build:
      name: Build Enjoy APK
          max_build_duration: 60

              environment:
                    android_signing:
                            - keystore_reference

                                scripts:
                                      - name: Build APK
                                              script: |
                                                        echo "Building Enjoy APK"

                                                            artifacts:
                                                                  - build/**/*.apk
